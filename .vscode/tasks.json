{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "avl2gtfsrt-dev",
            "type": "shell",
            "command": "docker compose -f docker-compose.yaml -f docker-compose.dev.yaml --env-file dev.env up -d --build worker",
            "group": "build",
            "problemMatcher": [],
            "isBackground": true
        }
    ]
}